package local.tilde.pw.linkedlists;

public class Node {
    int data; // value or data of linked list.
    Node next; // address of next node.

    Node(int data) {
        this.data = data;
    }
}
