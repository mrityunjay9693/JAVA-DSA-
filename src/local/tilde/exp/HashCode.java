package local.tilde.exp;

public class HashCode {
    public static void main(String[] args) {
        HashCode hashcode = new HashCode();
        HashCode hashCode1 = new HashCode();

        System.out.println("Hash Code " + hashcode + " : " + hashcode.hashCode());
        System.out.println("Hash Code " + hashcode + " : " + hashCode1.hashCode());
    }
}
